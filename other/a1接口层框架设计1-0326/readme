src6--ok  是可以跑通的（结构是2个文件，2个类）
src7只可以写入1条结果，
src8可以写入2条结果，--ok  分开2个类，但是2个类在一个文件
src9将两个类分开成2个文件--ok
src10将分出一个主函数main--ok（结构是4个文件，3个类）--将遍历case类，读取的字段作为列表的2个元组返回，用到了空列表
		--遍历case类中只有excel的读取，而把excel的写放在了接口类（这里可以改进，将excel的读写都放在遍历case类）
src11将在src6的基础上，将excel的读写都放在遍历case类，作为类的2个不同方法
src12梳理伪代码思路
src13代码精简，只保留伪代码思路
src14自己写伪代码和真代码-第一遍1
src15去掉注释，精简
src16在http请求类中，分析了r.json和json.loads(r)的区别，以及requests.post和requets.get函数的源码
     以及参数在新建对象的时候传入，在调方法的时候就不能传入了;捕捉异常Exception
     不运行的测试用例的测试结果置为空白
src18-第三遍练习
src24-http请求类、接口类、遍历case类，跑通了--推荐1
src25-只读取了一行，用于和src24的对比
src26-从excel读取的字段，确保去掉回车换行以及两端的空格--增强健壮性和容错性--推荐2
src27-加上了发邮件的功能
src28-加上了主入口文件
src29-主入口文件的遍历case类的方法增加2个参数，参数1-测试用例路径  参数2-测试报告路径
      主入口的发邮件的类方法增加了5个参数，发件人，收件人，邮件标题，测试报告路径，邮件正文
src30-主入口文件的遍历case类的方法增加2个参数，参数1-测试用例路径  参数2-测试报告路径
      主入口的发邮件的类方法增加了5个参数，发件人，收件人，邮件标题，测试报告路径，邮件正文
      a1_interface2.py中，遍历case类，增加2个形式参数--path2_case,path1_report
                        获取工作簿、获取工作表、获取最大行数放在遍历case类的普通方法中
      a1_interface2.py中，接口类，增加3个形式参数--path1_report,sh1,wb1
src31-将主入口文件从src目录中分离出来
        主入口文件单独出来，测试用例文件和测试报告文件和主入口文件同一级目录
        接口类文件、http请求类文件、发送邮件类文件都放在src1目录中
src32-将测试用例放到  存放测试用例的文件夹-cases--1
      将测试报告放到  存放测试报告的文件夹-reports--1
src33-将发送邮件文件1和http请求类放到公共函数文件夹
src34-将log代码，封装成类放到公共类库文件夹



0先实现一个文件写入1条excel记录

1再实现一个文件写入两条excel记录～for～ok
2然后考虑分开两个文件写入两条excel记录-for
做好总结
有耐心是关键  不怕慢就怕停
一步步调试print，切不可跳步骤～1

1空列表循环添加元素，空列表放在for外面
   append放for里面，print放for外面
2return放在for外面
3列表的元素是元祖时，不定长参数的星号*